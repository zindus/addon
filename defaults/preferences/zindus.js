pref("zindus.system.SyncGalMdInterval",      60);       // how frequently the entire GAL is request (seconds)
pref("zindus.system.preferSchemeForSoapUrl", "https");  // which scheme to prefer if multiple <soapURL>'s are recieved
pref("zindus.system.timerDelay",             86400);    // frequency schedule for the background sync timer (use 15 for debugging)

pref("zindus.loggingActive",          "true");    // turns logging on/off
pref("zindus.loggingFileSizeMax",     1000000);   // max size of a logfile
