pref("zindus.system.SyncGalMdInterval",      86400);    // how frequently the entire GAL is requested (seconds) (use 60 for debugging)
pref("zindus.system.timerDelay",             86400);    // frequency of the background sync timer (seconds)
pref("zindus.system.preferSchemeForSoapUrl", "https");  // which scheme to prefer if multiple <soapURL>'s are recieved
pref("zindus.system.logfileSizeMax",         3000000);  // logfile is truncated when it gets bigger than this
